
/**
 * il package {@code server} contiene le classi necessarie per modellare il server
 *
 * <ul>
 *     <li> La classe {@link server.MultiServer} si occupa di accettare connessioni da parte del client</li>
 *     <li> La classe {@link server.ServerOneClient} gestisce la connessione di un singolo client al server</li>
 * </ul>
 */

package server;
