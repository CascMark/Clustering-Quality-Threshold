
/**
 * il package {@code default_package} contiene le classi utili a definire la GUI e a stabilire la connessione al server
 *
 * <ul>
 *     <li> La classe {@link default_package.ClusClient} stabilisce la comunicazione con il server</li>
 *     <li> La classe {@link default_package.ControllerMain} gestisce l'interazione dell'utente tramite la GUI</li>
 *     <li> La classe {@link default_package.InvalidRadiusException} è un'eccezione personalizzata lanciata quando il raggio di clustering non è valido</li>
 *     <li> La classe {@link default_package.Main} si occupa dell'avvio della GUI e del caricamento del file FXML</li>
 *     <li> La classe {@link default_package.NoTextValues} è un'eccezione personalizzata lanciata quando non vi sono valori all'interno dei campi dell'applicazione</li>
 *     <li> La classe {@link default_package.ServerException} è un'eccezione personalizzata lanciata quando vi è un errore nella connessione al server</li>
 * </ul>
 */

package default_package;
